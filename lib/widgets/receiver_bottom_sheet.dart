import 'package:flutter/material.dart';

class ReceiverBottomSheet extends StatelessWidget {
  const ReceiverBottomSheet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
